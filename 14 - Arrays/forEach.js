// forEach apenas itera sob cada elemento do array, não retorna
// um novo array
const numbers = [3, 10, 60, 50, 24, 2, 1, 9, 59];

// for (let number of numbers) {
//     console.log(number)
// }

const teste = numbers.forEach(value => value);