let num1 = 9.54321;
let result;

// arredonda para baixo | floor = chão
// result = Math.floor(num1); 

// arredonda para cima | ceil = teto
// result = Math.ceil(num1);

// arredonda normalmente
// result = Math.round(num1);

// retorna o maior número da sequência fornecida
// result = Math.max(1, 2, 3, 4, 5, -10, -50, 1500, 9, 4, 6);

// retorna o menor número da sequência
// result = Math.min(1, 2, 3, 4, 5, -10, -50, 1500, 9, 4, 6);

// gera um número aleatório - multiplica (maior - menor) + menor
// result = Math.round(Math.random() * (10 - 5) + 5);

// potenciação
// result = 2 ** 10; 
// ou 
// result = Math.pow(2, 10)

// PI
// result = Math.PI;

  


console.log(result);